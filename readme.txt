这个软件是为安卓设计的一个录音机软件，需要支持鼠标操作和触摸操作，首先它可以正常进行录音得到完整的录音音频文件，然后可以对录音音频文件在本地进行语音转文字来得到一份字幕文件。这个录音机软件打开后分为左页和右页。左页为录音文件列表界面，右页为录音操作界面。左页这个录音文件列表界面显示所有的由我们软件进行录音得到
的录音文件，需要显示录音文件名称，录音时间日期，录音时长。每个文件自己独立的播放按钮和编辑录音文件名的编辑按钮还有两个转录按钮，分别为以中文模式进行转录和以英文模式进行转录。这个录音文件列表是我们软件
每次录音完成保存完成后在软件自己的数据库文件中记录此次录音的文件名和录音完成的日期和时间来形成的，每次录音得到的文件应当被储存在公有路径允许外界访问，并且用户在软件中修改录音文件名时应当将这个修改后的
录音文件名来覆盖掉现有的对应的文件的文件名和我们数据库里记录的文件名，来实现完整的文件名的更新。右页中应当可以点击开始录音按钮来开始录音，开始录音后开始按钮变为结束按钮，通过点击结束按钮来结束并按照默
认文件名规则自动保存音频文件。
在开始录音时应当在结束按钮旁边出现一个暂停按钮来暂停录制，并且应当实时显示当前录音时长。录音时使用的音质应当为192kps的码率，最终使用m4a格式通过Android MediaRecorder保存。
这个软件应当内置一个本地语音文件转录为文本（需要带句识别和时间戳记录）的本地语音转文字引擎，转录目标语言为英文和中文。在进入左页后再点击想要进行转录的语音的转录按钮后，应当使用软件内置的本地语音转文字
引擎进行转录，并且在通知栏发出通知提醒用户正在进行转录，也在软件中发出弹窗提醒用户当前正在转录和转录的当前状态。在转录完成后在通知栏再次发出通知提醒用户已经转写完成。转写完成时应当立刻将转写结果保存为
本地文件并在数据库记录此次转写。在检查到对应录音音频文件已经存在已经完成的转写结果文件时，点击转录按钮应当是进入转写结果查看界面。此时应当上方为录音播放器，占据手机上方约1/5的空间，具备暂停/继续，
步进(0.2秒)，步退（0.2秒）以及可以拖拽控制播放进度的进度条和实时播放时长/总时长的时间显示，而下方的空间显示多行字幕，并且按照时间戳将当前进度归属的那句字母高亮，同时需要支持滑动来查看上方/下方的字幕，
并且提供一个拖拽条来便于快速调整当前字幕查看进度，同时提供一个自动滚动按钮，点击自动滚动按钮后字幕将自动滚动使得当前进度对应的那一句字幕始终位于第二行。同时右上角需要提供删除此转录结果重新进行转录的按钮
，此按钮点击后应当提供二次确认框，在二次确认通过后才能删除当前进度并开始重新转录。转录的逻辑是，我们先使用MediaExtractor接受需要转录的音频文件，将其进行解压释放，然后我们使用MediaCodec对解压的内容
进行解码，得到原始PCM，原始PCM肯定不能在内存堆积，
必须先缓存为本地文件来回收内存，然后我们再将它使用TarsosDSP重采样为16000KHZ，缓存为标准PCM，然后我们再将这个完成了重采样的标准PCM提供给vosk，进行转录。转录完成之后和软件启动时，这些PCM本地缓存
文件都必须清除，而录音直接得到的m4a应当不受影响。
录音播放器，是一个GUI界面，在录音播放时和语音转写结果查看界面承担音频播放相关的人机交互作用，具备暂停/继续，步进(0.2秒)，步退（0.2秒）以及可以拖拽控制播放进度的进度条和实时播放时长/总时长的时间显示。
我们需要在录音列表中的play按钮点击后对这个相应的录音文件由音频播放器承担这个播放相关的任务。
而在语音转写成功后进入查看语音转写内容时，也靠它进行播放控制。
在与模型对话界面使用Tavily API来为模型加入了联网检索功能，并借助claude opus 3,5删除了不必要的一些代码。（2026/4/1）
我们为软件加入了讯飞标准版音频转录的联网转录能力，需要用户手动输入自己的APPID和secret key，并且加入了qwen3max联网深度思考模型，需要用户手动输入自己的API KEY,并且 加入了音频导入和音频分享（导出
）功能。
我们加入了利用Qwen omni plus的音频总结功能，目前设置为限制时长30Min以内，文件最大分析大小100MB
❯ 现在，我们要让软件在与模型对话的界面中加一个按钮，代表“本地内容同步”，在启用联网对话模型后变为可用状态，实际上按下这个按钮后应当触发对于当前整个文件列表中的所有音频各自触发进行音频总结，因为部分
文件可能会失败，所以不同文件之间应该是并行结构，避免单个失败导致全部失败。然后再使用qwen 3 max
  对于所有没有经二次总结结果但是有有效第一次总结结果的文件对他们的直接总结结果再次总结到125字以内，这个二次总结结果不应当出现在前台的任何一个界面中。然后软件应当在内部使用一个json文件，储存整个当前
  的文件列表中每个文件的文件名，时长，时间，还有（如果有的话）每一个完成了二次总结的文件的二次总结的内容。（在
  每次启动软件和启动后的每10分钟和点击“本地内容同步”按钮时进行刷新更新，检查是否有文件被移除出文件列表，并更新文件的这些相关信息，以及确保新文件的信息也被加入）
  然后在与模型对话的界面，当选择了在线模型（qwen3MAX)的情况下，在联网搜索这个选项下面再加上“本地文件记忆”这个选项，如果勾选了这个选项，则在用户对话时先向云端模型输入这个json文件的内容，然后再输入
  用户的输入内容（这两个内容组成了完整的单次输入）。同时在设置里加入“减少本地文件记忆token消耗（测试版）”和“强制
  重建本地内容同步数据库（谨慎！）”两个选项，“减少本地文件记忆token消耗”选项勾选时我们的对话在开启了“本地文件记忆”后在每轮对话中只有这一轮对话的第一次输入才会使用本地文件索引json加用户实际输入的
  形式，这轮对话的第二次以及后续输入不再在用户的输入前面插入本地文件索引json，
  在点击了“强制重建本地同步数据库”选项后，先弹出2次再确认框，两次要求用户明白一旦重建会大量消耗qwen omni plus和qwen 3
  max额度并且消耗大量时间和流量，可能造成较多资金损失。如果用户这两次确定都确认后，删除所有的二次总结结果，然后询问用户是否删除第一次总结结果，如果此时用户确认删除第一次总结结果，则删除所有第一次总结
  结果，然后重新进行本地文件同步，如果用户选择不删除第一次总结结果，则直接进行本地内容同步。
  需要注意，”本地文件同步“功能必须要基于目标文件没有对应的总结内容（第一次，第二次）且文件小于20MB才可以去执行音频总结，而且对于每一个文件的每一次总结前都必须检查和确认没有有效音频总结结果并且小于
  20MB才能允许总结。且第二次总结也是如此，而且我们是允许部分文件没有办法进行第一次总结的，这种情况下我们的索引
  依然记录着文件列表中出现的这些文件名，时长，文件产生日期三大要素。(2026/4/19, Claude opus4.7 提示词）
使用claude opus 4.7进行了全面的UI重构，极大优化了观感，并改善了多种分辨率兼容效果。 2026/4/20 ， claude opus 4.7